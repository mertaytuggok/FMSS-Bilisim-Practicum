import React from "react";
import styles from "./Header.module.css";

const Header = () => {
  return (
    <div className={styles.description}>
      <p>Book Search using React</p>
    </div>
  );
};

export default Header;
